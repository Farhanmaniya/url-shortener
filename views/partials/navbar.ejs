<nav class="bg-white border-b border-slate-200 px-6 py-4">
    <div class="max-w-4xl mx-auto flex items-center justify-between">
        <a href="/" class="text-xl font-bold text-blue-600 tracking-tight">
            shortly
        </a> 
        <div class="flex items-center gap-4">
            <% if (locals.userId) { %>
                <a href="/api/mylinks"
                    class="text-sm text-slate-600 hover:text-slate-900 transition-colors">
                    My Links    
                </a>
                <form method="POST" action="/user/logout">
                    <button type="submit" class="text-sm bg-slate-100 hover:bg-slate-200 text-slate-700 px-4 py-2 rounded-lg transition-colors">
                        Logout
                    </button>
                </form>
            <% } else { %>
                <a href="/login" class="text-sm text-slate-600 hover:text-slate-900 transition-colors">
                    Login
                </a>
                <a href="/signup" class="text-sm bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition-colors">
                    Sign Up
                </a>
            <% } %>
        </div>
    </div>
</nav>